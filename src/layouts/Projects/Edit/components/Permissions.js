import React from "react";
import { ErrorBoundary, Lang } from "fogito-core-ui";

export const Permissions = ({ state, setState }) => {
  return (
    <ErrorBoundary>
      <div className="row"></div>
    </ErrorBoundary>
  );
};
